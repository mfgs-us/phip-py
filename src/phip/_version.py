"""Version constants for phip-py.

The library version (`__version__`) and the spec version it implements
(`PROTOCOL_VERSION`) move on independent SemVer trajectories per the PhIP
versioning policy. Both are updated together when the spec changes the
wire format.
"""

__version__ = "0.1.0a3"
PROTOCOL_VERSION = "0.1.0-draft"
